---
type: page
title: Home
date: 2021-03-19
---

import Parser from "rss-parser";
import Link from "next/link";


export default () => {
    const items = parser.parseURL("https://blog.openduct.net/feed.xml").then((res) => res.items);
    return (
        <>
            {items.map((item) => (
            <a
                key={item.link}
                href={item.link}
                target="_blank"
                rel="noopener noreferrer"
            >
                <div>{item.title}</div>
            </a>
            ))}
        </>
    )
}
